{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["$TURBO_DEFAULT$", ".env*"],
      "outputs": ["dist/**"]
    },
    "lint": {},
    "format": {},
    "format:fix": {},
    "lint:fix": {},
    "lint:ci": {},
    "format:ci": {},
    "test": {
      "inputs": ["**/*.test.ts"]
    },
    "dev": {
      "cache": false,
      "persistent": true
    }
  }
}
